name: Pipeline de Medios — Homicidios

on:
  schedule:
    # Corre todos los días a las 7am hora Argentina (UTC-3)
    - cron: '0 10 * * *'
  workflow_dispatch:  # permite correrlo manualmente desde GitHub

jobs:
  pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Instalar Chromium para agent-browser
        run: npx agent-browser install

      - name: Generar Prisma Client
        run: npx prisma generate
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      - name: Verificar variables de entorno
        run: |
          echo "OPENROUTER_API_KEY set: ${{ secrets.OPENROUTER_API_KEY != '' }}"
          echo "DATABASE_URL set: ${{ secrets.DATABASE_URL != '' }}"
          echo "OPENROUTER_MODEL: ${{ secrets.OPENROUTER_MODEL }}"

      - name: Correr pipeline
        run: npm run pipeline:run
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          OPENROUTER_MODEL: ${{ secrets.OPENROUTER_MODEL }}
          PIPELINE_DRY_RUN: 'false'
          PIPELINE_MAX_NOTICIAS: '10'
          PIPELINE_PERFIL_MODELO: 'economico'

      - name: Notificar resultado
        if: always()
        run: |
          echo "Pipeline completado con estado: ${{ job.status }}"
